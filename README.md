# Learning_Python
The program in student_grade_analyzer:

Reads a CSV file containing student names and grades.

Calculates the average grade.

Finds the maximum grade.

Prints the names of students whose grades are above the average.